# Kaku2Okur Tauri Legacy

This directory preserves the original Tauri 2, React, and Konva implementation.
It remains independently runnable with:

```sh
npm run tauri dev
```

The active lightweight native implementation lives in `../native`.

